package io.pryce.android.autospeed.core.settings

/** The orientation setting (design section 3.3). */
enum class OrientationMode {
    LANDSCAPE,
    PORTRAIT,
    AUTO_SELECT,
}

/** The background setting (design section 3.7). */
enum class BackgroundMode {
    SYSTEM_WALLPAPER,
    SOLID_COLOR,
    IMAGE,
}
