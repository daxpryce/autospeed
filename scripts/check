#!/usr/bin/env bash
set -euo pipefail

readonly ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT}"

./gradlew --no-daemon \
  ktlintCheck \
  detekt \
  lint \
  test \
  koverVerify
typos
