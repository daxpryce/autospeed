<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Precise foreground location to calculate current speed (design section 4.2). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Continue speed display in overlay mode (design section 4.2). Background location is
         never requested: overlay mode is an explicitly started, continuously visible foreground
         operation, not passive background tracking. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Show the required foreground-service notification when starting overlay mode. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Render the Autospeed surface over a selected companion application. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Suppress notification interruptions through an Autospeed-owned Zen rule. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Autospeed makes no network requests; every build omits INTERNET (design section 4.1). -->

    <!--
        Narrow package visibility for the map/media companion pickers (design section 3.5): only
        applications that resolve a map-viewing intent or declare themselves a music/audio app are
        visible to Autospeed. This deliberately avoids QUERY_ALL_PACKAGES and avoids a broad
        MAIN/LAUNCHER query, which would expose every installed application.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_MUSIC" />
        </intent>
    </queries>

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:name=".AutospeedApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Autospeed"
        android:hasFragileUserData="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.Autospeed">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".setup.SetupActivity"
            android:exported="false"
            android:theme="@style/Theme.Autospeed.Content" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Autospeed.Content" />

        <service
            android:name=".media.AutospeedNotificationListenerService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".overlay.OverlayService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <service
            android:name=".notification.AutospeedZenConditionProviderService"
            android:exported="true"
            android:label="@string/zen_condition_provider_label"
            android:permission="android.permission.BIND_CONDITION_PROVIDER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.ConditionProviderService" />
            </intent-filter>
        </service>

    </application>

</manifest>
