<?xml version="1.0" encoding="utf-8"?>
<!--
    Design section 3.8: there is no backup requirement, and settings/selected identifiers must not
    leave the device through Android backup or device transfer. Both cloud backup and device-to-
    device transfer therefore exclude the entire application domain. android:allowBackup="false"
    already disables backup; these rules make the same intent explicit on Android 12 and higher.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
